// Every
// return true if every element of array gives true for Every function.Else return false 

// arr.every(some function definition or name);
//Every return true or false

//Every is similar to that of the logical and operator
console.log([1,2,3,4].every((ele)=>ele%2==0));

//Some
// return true if some element of array gives true for some function.Else return false 
// arr.some(some function definition or name);
//some return true or false

console.log([1,2,3,4].some((el)=>(el%2==0)));